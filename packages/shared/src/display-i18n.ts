/**
 * Per-locale display strings from API (additive, read path). Canonical DB fields stay source of truth.
 */
export type ValueTranslations = {
  ar?: string | null;
  en?: string | null;
  he?: string | null;
};
