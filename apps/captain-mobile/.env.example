# Copy to `.env`. Use your API origin only (no `/api` suffix).
# Local API (same machine): http://127.0.0.1:4000
# Android emulator → host: http://10.0.2.2:4000
# Production: set in CI or your hosted API URL
EXPO_PUBLIC_API_URL=https://YOUR_API_DOMAIN
EXPO_PUBLIC_SOCKET_URL=https://YOUR_API_DOMAIN
